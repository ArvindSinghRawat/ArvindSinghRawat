name: Update badges

on:
  schedule:
    # Runs at 2 am UTC
    - cron: "0 12 * * *"

  workflow_dispatch:

jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:       
          CREDLY_USER: arvind-singh-rawat
